{
  "name": "@wikibridge/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types generated from FastAPI OpenAPI schema",
  "scripts": {
    "generate": "echo 'OpenAPI client generation — available from Phase 1 when API schema stabilizes'"
  },
  "main": "src/index.ts",
  "types": "src/index.ts"
}
