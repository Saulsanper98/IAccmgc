{
  "name": "wikibridge",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "generate:api-client": "npm run generate --workspace=packages/shared"
  },
  "engines": {
    "node": ">=20"
  }
}
